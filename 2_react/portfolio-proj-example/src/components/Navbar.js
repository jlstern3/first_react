import React from 'react'; // this is the React library that exists in the node_modules folder

const Navbar = () => {
    return(
        <div>
            <button>Home</button>
            <button>Projects</button>
            <button>About Me</button>
            <button>Contact</button>
        </div>
    )
}

export default Navbar;
